import React from 'react';
import './YCLoader.css'; // Assuming you saved the CSS in Spinner.css


const YCLoader: React.FC = () => {
    return (
        <div
            className="spinner"
        />
    );
};

export default YCLoader;
